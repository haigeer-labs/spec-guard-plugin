# 本地工作流后续审阅

审阅基线：51f242b。范围：本地上下文入口、历史分类、生命周期快照、检查点引用及其回归。没有启动额外代理或宿主模型评测。

## 发现并修复

本地 setup 在未提供 CLAUDE_PROJECT_DIR 时直接使用 cwd，与普通 setup 的 Git 根目录解析不一致。
从 nested 子目录调用会报“能力图缺失或结构不合法”，即使项目根已有完整产物。

新增普通临时目录与 Git 根解析桩的测试，修复前退出 1，修复后通过；断言根 state 更新成功、子目录没有新增 .agent。
修复仅在本地入口分流前使用原有根目录解析方式并导出 CLAUDE_PROJECT_DIR。显式指定项目根仍优先。
本地上下文测试由 6 项增至 7 项。

## 验证环境问题

初次总校验在 test-checkers 的变异安全闸测试失败：测试临时追加 spec-digest.py 注释后，
git checkout 恢复需要写入当前 worktree 的 Git 索引，受沙箱权限限制而失败。
已逐字节确认该文件只多出测试标记，恢复原内容；没有修改指纹算法。
随后单独允许该本地测试的索引清理权限，重新执行总校验。五项规定检查最终退出码均为 0；本地聚焦测试共 29 项通过（检查点的 2 项仅验证静态引用）。
完整命令与日志见 [回归证据](local-workflow-review-validation-20260906.json)。
原受限运行日志保留在 /private/tmp/sg-review-validation/；失败不计作通过。

## 边界与下一步

本轮仅完成本地审阅和范围内修复，未推送、发布、修改缓存或重启宿主。
旧安装版的 SPECED 提示不能作为同步依据。真实宿主交互与安装更新验收仍未完成。
下一步是准备具体版本及发布/安装操作预览；预览不执行外部写入，预览完成后再确认具体操作范围。
